import AdminPanel from "@/components/admin/AdminPanel";

export const metadata = {
  title: "لوحة التحكم | a3mal watad",
  robots: "noindex, nofollow",
};

export default function AdminPage() {
  return <AdminPanel />;
}
